<% content_for(:title, "#{@user.name}のフォロー") %>

<div class="row">
  <%= render 'user_profile' %>
  <div class="col-md-10 col-md-offset-1 mt-30">
    <%= render 'user_link' %>

    <%= render 'user_following' %>
  </div>
</div>